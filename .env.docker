# Docker Configuration for Image Gallery
# Copy this to .env and modify as needed

# Flask Configuration
SECRET_KEY=your-very-secure-random-secret-key-here
FLASK_DEBUG=false

# Database Configuration
DATABASE_TYPE=postgresql
# Use DATABASE_TYPE=sqlite for simple deployment

# PostgreSQL Configuration (for docker-compose.yml)
POSTGRES_DATABASE=imagedb
POSTGRES_USER=gallery_user
POSTGRES_PASSWORD=secure_password_change_this

# Application Settings
MAX_UPLOAD_SIZE=16777216
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif
DEFAULT_PAGINATION=8
MAX_IMAGES=100

# LAION Dataset Configuration
MAX_WORKERS=8
DOWNLOAD_TIMEOUT=15
